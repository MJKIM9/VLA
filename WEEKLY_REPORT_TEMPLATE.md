<!--
================================================================
진행 보고 템플릿 (상세본) · Claude Code 자동 생성
================================================================
"/weekly-report" 로 실행하면 Claude Code가 아래 항목을 모두 자동으로 채운다.
- git log --oneline, git diff --stat 을 기본 근거로 삼는다.
- 개발 내용을 설명하는 데 필요하면 관련 코드를 분석한다.
- 코드와 커밋에서 확인되는 내용만 적고, 확인되지 않는 것은 넣지 않는다.
- 원본을 덮어쓰지 말고 reports/WEEKLY_YYYY-MM-DD.md 사본으로 저장한다.
================================================================
-->

# 진행 보고서

| 항목 | 내용 |
|------|------|
| 담당자 | (git 사용자 이름) |
| 과제 | (워크스페이스 또는 브랜치 기준) |
| 보고 기간 | YYYY-MM-DD ~ YYYY-MM-DD |
| 작성일 | YYYY-MM-DD |

---

## 1. 이번 기간 요약
이번 기간에 개발한 내용을 두세 문장으로 요약.

---

## 2. 개발 내용 상세

### 원리
무엇을 어떤 방식과 원리로 구현했는지 서술.

### 구성
모듈, 클래스, 파일이 어떻게 구성되는지 서술.

### 순서 및 단계
처리 흐름 또는 개발 단계를 순서대로 서술.

### 패키지 구성
디렉터리 및 패키지 구조를 정리. 필요하면 트리로 표현.

---

## 3. 변경 근거 (git)

**커밋 로그**
```
git log --since="7 days ago" --oneline 출력
```

**변경 요약**
```
git diff --stat 출력
```
