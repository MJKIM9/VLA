<!-- >>> 자동화팀 공통 지침 시작 (자동 관리 영역, 직접 수정하지 마세요) >>> -->
## 자동화팀 공통 지침 (Claude Code 자동 로드)

이 영역은 팀 공통 지침이며 scheme_update.sh 로 자동 갱신됩니다. 이 블록은 직접 수정하지 마세요.
이 블록 밖에는 각자 필요한 규칙을 자유롭게 추가할 수 있습니다.

### 진행 보고 작성
- 진행 보고는 `/weekly-report` 로 생성한다. 상세본과 발표용 2종이 함께 생성된다.
- 보고서는 사람이 손으로 채우지 않는다. Claude Code가 코드와 git 기록을 분석해 전체를 자동으로 작성한다.
- git 명령 출력을 기본 근거로 삼되, 개발 내용을 설명하는 데 필요하면 관련 코드를 분석한다.
- 보고서에는 이번 기간에 개발한 내용을 상세히 담는다. 특히 다음을 포함한다.
  - 원리: 무엇을 어떤 방식과 원리로 구현했는가
  - 구성: 모듈, 클래스, 파일이 어떻게 구성되는가
  - 순서 및 단계: 처리 흐름 또는 개발 단계
  - 패키지 구성: 디렉터리 및 패키지 구조
- 코드와 커밋에서 확인되는 내용만 적는다. 확인되지 않는 것은 넣지 않는다.
- 템플릿 두 파일(`WEEKLY_REPORT_TEMPLATE.md`, `WEEKLY_BRIEFING_TEMPLATE.md`)은 수정하지 않는다.
- 결과물은 `reports/WEEKLY_YYYY-MM-DD.md`, `reports/BRIEFING_YYYY-MM-DD.md` 사본으로 저장한다.
- 생성한 보고서 파일은 git에 커밋해 기록으로 남긴다. 보고서는 저장소에 보존한다.

### 문서 표기 규칙
- 가운뎃점을 쓰지 말고 쉼표 또는 '및'으로 표기한다.

### Git 관리
- 로컬 저장소: 변경사항이 생길 때마다 커밋한다. 커밋 메시지에 무엇을 왜 바꿨는지 변경 내용을 함께 적는다.
- 커밋 메시지는 한글로 한 줄로 명확히 쓴다. 이 메시지가 보고서의 근거가 되므로 "수정", "작업" 같은 모호한 말은 피한다.
  - 좋은 예: `2지 그리퍼 유격 원인 파악 및 수정 설계 반영`
  - 나쁜 예: `update`, `수정함`, `wip`
- 원격 저장소: 하나의 의미 있는 작업이 완성되면 원격에 push 한다.
<!-- <<< 자동화팀 공통 지침 끝 <<< -->
